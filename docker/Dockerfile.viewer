FROM node:18-alpine

WORKDIR /app

# Install express
RUN npm init -y && npm install express

# Create directories
RUN mkdir -p /app/captured /app/configs

EXPOSE 8090

CMD ["node", "/app/viewer-server.js"]